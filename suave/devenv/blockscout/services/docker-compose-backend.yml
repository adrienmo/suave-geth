version: '3.8'

services:
  blockscout-backend:
    image: adrienmo/blockscout:suave4
    pull_policy: always
    restart: always
    container_name: 'blockscout-backend'
    command: sh -c "bin/blockscout eval \"Elixir.Explorer.ReleaseTasks.create_and_migrate()\" && bin/blockscout start"
    env_file:
      -  ../envs/common-blockscout.env
    volumes:
      - ./logs/:/app/logs/
